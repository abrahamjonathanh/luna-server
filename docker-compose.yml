version: '3.9'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/luna
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db
    environment:
      - APP_NAME = 'Luna'
      - DB_NAME = 'explore'
      - DB_USER = 'postgres'
      - DB_PASSWORD = 'admin'
      - DB_HOST = 'db'
      - DB_PORT = '5433'
      - DB_SCHEMA = 'explore'
      - EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
      - EMAIL_HOST = 'smtp.gmail.com'  
      - EMAIL_PORT = 587
      - EMAIL_USE_TLS = True
      - EMAIL_HOST_USER = 'abrahamjonathan.acc@gmail.com'
      - EMAIL_HOST_PASSWORD = 'tlwpcklimsjjpnaa'
      - FERNET_KEY = 'EyecKs0adF_6EZ6PDz-10wcVRQlnQRgRIUt51yKNiXM='

  # pgadmin:
  #   image: dpage/pgadmin4
  #   container_name: pgadmin
  #   restart: always
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: admin@admin.com
  #     PGADMIN_DEFAULT_PASSWORD: admin123
  #   ports:
  #     - "5050:80"  # Access pgAdmin via http://localhost:5050
  #   depends_on:
  #     - db

  db:
    image: postgres:15
    restart: always
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: explore
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin

volumes:
  postgres_data:
